# This loop continuously asks the user to enter a name and prints the entered name.
# This is a infinite loop
while True:
    # asks the user to enter a name and store it in the 'name' variable
    name = input("Enter name: ")
    
    # Print the entered name
    print(name)
